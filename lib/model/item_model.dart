class ItemModel{
  String image;
  String name;
  String title;
  String about;
  String price;
  bool favourite;
  int cardCount;

  ItemModel(this.image, this.name, this.title, this.about, this.price);
}